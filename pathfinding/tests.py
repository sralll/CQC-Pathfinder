"""Tests for the navgraph (visibility-graph) module have been removed
together with the module itself. Pathfinding now lives client-side in the
Web Worker under ``static/js/pathing/``; smoke tests for that pipeline are
in the project's frontend test harness, not Django."""
