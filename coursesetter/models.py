# Deprecated app — all models removed. Data lives in the `project` app.
# The app is kept installed only until the DeleteModel migrations have run
# everywhere; after that the whole directory is removed.
